{% extends 'base.html.twig' %}
{% trans_default_domain 'FOSUserBundle' %}

{% block main %}
    <div class="row">
        <div class="col-md-2"></div>
        <div class="col-md-8 center" style="margin-top:40px;">
            <p class="short lead">{{ 'registration.confirmed'|trans({'%username%': user.username}) }}</p>
            <p class="short lead">
                Hiện tại bạn có thể tự tạo thiệp của mình, chỉnh sửa và thay đổi mẫu thiệp bất cứ lúc nào.
            </p>
            <div class="col-md-2"></div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-1"></div>
        <div class="col-md-10 center" style="margin-top:40px;">
            <p>
                <a href="{{ path('templates_page') }}">Mẫu thiệp</a>
            </p>
            <div class="col-md-1"></div>
        </div>
    </div>
{% endblock %}
